<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- ip-api.com is HTTP-only on the free tier; allow cleartext for it (used to classify a
         proxy exit IP as Mobile / Residential / Datacenter). Everything else stays HTTPS-only. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">ip-api.com</domain>
    </domain-config>
</network-security-config>
